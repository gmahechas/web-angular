import { FetchStringInArraySharedPipe } from './fetch-string-in-array-shared.pipe';

describe('FetchStringInArraySharedPipe', () => {
  it('create an instance', () => {
    const pipe = new FetchStringInArraySharedPipe();
    expect(pipe).toBeTruthy();
  });
});
